# 70-Sandbox · 实验 / 原型

短命的实验性内容。**默认不参与多机同步**，也默认被本仓库 `.gitignore` 忽略（除本说明文件）。

## 使用建议

- 一个想法一个目录，命名 `YYYY-MM-DD-<idea>/`。
- 验证后：
  - **保留**：迁到对应 Phase 或 `20-Claude-Code/skills/`。
  - **淘汰**：直接删除，或在 `60-Repo-Research/reject-list/` 留一句结论。
- 不要在 Sandbox 里放任何长期依赖（密钥、生产数据、长寿配置）。
